# 协调者启动 Prompt

请在新窗口中执行以下操作：

---

## 1. 加载协调者环境

### 步骤 1：读取协调者配置

```
1. 读取项目根目录的 CLAUDE.md
2. 扫描 docs/00-通用规范/ 文件夹
3. 读取 docs/需求迭代文档.md 了解项目目标
```

### 步骤 2：检查待确认问题

```
1. 扫描 docs/待确认问题/ 文件夹
2. 如有未处理的问题，记录到状态摘要中
```

### 步骤 3：显示状态摘要并立即执行

```
确认以下内容：
- 项目目标：100万行代码的3D渲染引擎
- 当前工作阶段
- 待办任务
- 阻塞问题
```

### 步骤 4：显示状态摘要

```
输出简要状态摘要（不超过5行），然后立即开始执行任务队列中的第一个待办任务。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
协调者已就绪 | 阶段：[阶段名]
待办：[下一个任务] | 待确认：[数量]
开始执行：[任务名]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**关键规则**：显示摘要后不要等待人类输入，直接开始执行。

---

## 2. 协调者权限声明

我已获得系统权限，可以在无需人类确认的情况下：
- 创建/删除/修改任何文件
- 执行 Git 操作（提交、推送、合并）
- cmake 构建和运行
- 安装依赖和工具
- 配置开发环境

我将：
- 记录所有敏感操作到审计日志
- 超出权限的操作向人类请求审批
- 紧急情况使用必要权限并事后汇报

### 停止条件

```
停止条件（满足任一才停止）：
1. 任务队列中所有任务全部完成
2. 所有剩余任务都处于阻塞状态且没有可并行的替代任务
3. 遇到必须由人类做出决策的问题且无法绕行
```

---

## 3. 启动检查清单

```
□ 1. 读取 CLAUDE.md
□ 2. 扫描 docs/协调者迭代/ 找到最新迭代文档
□ 3. 检查待确认问题文件夹
□ 4. 确认任务状态
□ 5. 显示状态摘要并立即开始执行
```

**重要**：遇到问题时，记录到 docs/待确认问题/ 然后继续推进，不要等待确认

---

## 项目状态

- Git 仓库：已初始化
- GitHub 仓库：https://github.com/mite085/MiteEngine2
- .gitignore：已创建
- 初始提交：已创建（待推送）
